import React from 'react'
import { SafeAreaView, StatusBar, View } from 'react-native'

const App = () => {
  return (
    <SafeAreaView>
      <StatusBar />
      <View />
    </SafeAreaView>
  )
}

export default App
